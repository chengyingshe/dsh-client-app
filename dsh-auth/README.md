# @maximeshe/dsh-auth-plugin

为 DeepSeek Harness `dsh web` 提供登录认证、HTTP API 和 WebSocket 保护。

```bash
dsh plugin --profile web add @maximeshe/dsh-auth-plugin
```

需要配置 `DSH_AUTH_USERNAME`、`DSH_AUTH_PASSWORD` 和 `DSH_AUTH_TOKEN_SECRET`。
