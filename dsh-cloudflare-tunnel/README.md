# @deepseek-ai/dsh-cloudflare-tunnel-plugin

可选的 Cloudflare Tunnel 插件，可独立于认证插件运行。

```bash
dsh plugin --profile web add @deepseek-ai/dsh-cloudflare-tunnel-plugin
```

使用 `CLOUDFLARE_TUNNEL_TOKEN` 和可选的 `DSH_TUNNEL_TARGET` 配置 Tunnel。
